name: 'adtech'
version: '1.0.0'
profile: 'adtech'

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
test-paths: ["tests"]
snapshot-paths: ["snapshots"]

models:
  adtech:
    staging:
      +materialized: view
    marts:
      +materialized: table
